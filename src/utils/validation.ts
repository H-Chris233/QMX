/**
 * 验证工具 - 占位实现
 * TODO: 实现实际的验证逻辑
 */

/**
 * 验证学员表单数据
 * @param _data - 表单数据
 * @returns 验证结果
 */
export function validateStudentForm(_data: unknown): { isValid: boolean; errors: string[] } {
  return { isValid: true, errors: [] };
}

/**
 * 验证交易表单数据
 * @param _data - 表单数据
 * @returns 验证结果
 */
export function validateTransactionForm(_data: unknown): { isValid: boolean; errors: string[] } {
  return { isValid: true, errors: [] };
}
